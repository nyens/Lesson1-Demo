# Lesson1-Demo
First task creating Repos
<br>
Author- Arun Sahani
<br>
check version git --version
<br>
check To list all files currently being tracked by Git in your repository ls
<br>
show all file ls -a
<br>
check working directory pwd
<br>
clear the window clear command
<br>
git config --global user.name"NAME"
<br>
git config --global user.email"EMAIL"
<br>
git config --list
<br>
clone - cloning a repos in our local machine
<br>
git clone <link from github>
<br>
cd- change directory: when we do something on local machine and that refelect on github
cd <main file name>-inside folder name
<br>
status- display the state of code
<br>
git status
